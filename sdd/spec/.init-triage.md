# Init Triage

Items raised during `/sdd init` Import Mode that need user decisions before the spec can be considered complete. Each item is resolved interactively via `/sdd init` Resume Mode.

While any `**Status:** open` items remain, `sdd/spec/config.yml` carries `transition: true` and PR-boundary review enforcement is suspended for the transition.

---

## TRIAGE-001: Legacy login surface appears unreferenced

**Status:** resolved-corrected (2026-06-01)

**Target REQ:** REQ-SESS-001 / REQ-BOOT-002

**Context:** `lib/main.dart` imports `lib/screens/simple_login_screen.dart` and routes unauthenticated users to `SimpleLoginScreen`. `lib/screens/login_screen.dart` defines a separate `LoginScreen`, but current-source grep finds no inbound reference from startup, home, or settings navigation. Prior closed PR context also treated this file as legacy, but that PR did not merge.

**Recommendation:** Confirm whether `lib/screens/login_screen.dart` is dead code. If yes, archive or delete it in a dedicated cleanup change; if no, wire it into navigation and update the Server Session requirements.

**Resolution:** Corrected by user as a future feature, not dead code and not current product behavior. Current requirements remain anchored to `SimpleLoginScreen`; the alternate login surface stays outside implemented scope until a future planned requirement wires it into navigation.

---

## TRIAGE-002: Short-link compatibility cascade needs a support-policy decision

**Status:** open

**Target REQ:** REQ-LINK-001

**Context:** `lib/services/file_upload_service.dart` method `shortenUrl` tries multiple request shapes and fallback endpoints before returning a normalized short-link result. Source does not explain the minimum server version or which fallback is still needed.

**Recommendation:** Decide the minimum supported server API shape. Keep and document the cascade if older deployments are supported; otherwise remove unsupported fallbacks in a dedicated cleanup change.

---

## TRIAGE-003: File protection action is best-effort

**Status:** open

**Target REQ:** REQ-REMOTE-002

**Context:** `lib/services/file_upload_service.dart` method `setFilePassword` tries multiple endpoints and returns `false` after failures. `lib/screens/home_screen.dart` exposes a protection action, but the failure path does not clearly tell the user that the remote item stayed unchanged.

**Recommendation:** Confirm whether file protection is supported by the target server. If supported, document the supported endpoint and surface failures to the user; if unsupported, remove or hide the file protection affordance.

---

## TRIAGE-004: Post-upload expiration mutation is not implemented

**Status:** open

**Target REQ:** REQ-REMOTE-003

**Context:** `lib/services/file_upload_service.dart` method `setFileExpiration` logs that post-upload expiration modification is unsupported and always returns `false`. Any future UI that calls this method would create a no-op user action.

**Recommendation:** Keep expiration editing out of the official product surface unless upload-time expiration is implemented. If the method is retained as a placeholder, document it as pending rather than as supported behavior.

---

## TRIAGE-005: Production package identifier is still template-shaped

**Status:** open

**Target REQ:** REQ-BOOT-001 / CON-PLATFORM-001

**Context:** `android/app/build.gradle` sets `applicationId` and namespace to `com.example.zipline_native_app`; `android/app/src/main/kotlin/com/example/zipline_native_app/MainActivity.kt`, `lib/services/intent_service.dart`, and `cloudflare-oauth-redirect/src/worker.js` also contain that identifier or derived channel names. The build file includes a TODO to choose a unique application id.

**Recommendation:** Choose the production identifier before release distribution and update the build file, Kotlin package path, method-channel namespace, manifest expectations, and worker fallback together.

---

## TRIAGE-006: README theme claim conflicts with source behavior

**Status:** open

**Target REQ:** REQ-THEME-001

**Context:** `README.md` describes dark mode as if light mode was not implemented. Source files `lib/core/constants.dart`, `lib/providers/theme_provider.dart`, and `lib/screens/settings_screen.dart` define and expose light, dark, and system-capable theme behavior.

**Recommendation:** Decide whether light mode is supported. If supported, update README copy and screenshots; if not, remove the toggle and light theme code.

---

## TRIAGE-007: Prior scaffold PR was closed as fabricated

**Status:** open

**Target REQ:** SDD import process

**Context:** GitHub PR #3 was closed by the owner with the note that the scaffold contained fabricated content. It is useful only as warning context and must not be treated as source truth.

**Recommendation:** Ignore PR #3 generated claims unless current source or tests independently anchor them.

---

## TRIAGE-008: Later scaffold PR contains unmerged decisions

**Status:** open

**Target REQ:** SDD import process

**Context:** GitHub PR #4 contains a later scaffold and resolved triage decisions, but the PR is closed and unmerged on `main`. Some decisions may still reflect the user's intent, but accepting them silently would import off-branch policy.

**Recommendation:** During Resume Mode, ask which PR #4 decisions should be accepted into the official spec and which should be re-decided from current source.
